<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the signed app AND every nested Node
  executable (Resources/bin/node runs the server; V8 JITs in both).
  - allow-jit + allow-unsigned-executable-memory: V8/JSC JIT pages.
  - allow-dyld-environment-variables: Electron relaunch paths set DYLD_*.
  - disable-library-validation: the bundled Node dlopens node-pty's prebuilt
    pty.node; without this, library validation rejects it under the hardened
    runtime even when both are signed by the same team.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
