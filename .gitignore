node_modules
dist/
.DS_Store
.env
.env.local
*.log
coverage/
.vite/
packages/*/dist/
*.pre-workspace-bak
config/*.bak

# Live config is machine-local (see config.example/); .backups/ dirs are the
# app's own rotating safety net.
/config/
packages/server/config/

# packaging artifacts (scripts/package-mac.mjs)
build/
release/

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Strado runs Shell tabs with ZDOTDIR pointed here; zsh would drop history in it
packages/server/hooks/shell/zsh/.zsh_history
